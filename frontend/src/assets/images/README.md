# Image Assets

Use this folder for image files that are imported by React components.

- `logos/` stores brand and logo images.
- Add more folders later when needed, such as `backgrounds/`, `icons/`, or `illustrations/`.
